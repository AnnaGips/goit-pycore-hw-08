# Використаємо офіційний базовий образ Python версії 3.9 slim
FROM python:3.9-slim


# Встановимо робочу директорію контейнера
WORKDIR /app

# Скопіюємо всі файли з поточної директорії в робочу директорію контейнера
COPY . /app

# Встановимо залежності Python з файлу requirements.txt
RUN pip install -r requirements.txt

# Вказуємо, який файл слід виконати при запуску контейнера
CMD ["python", "hm_8.py"]